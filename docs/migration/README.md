# Migration Documentation

This directory tracks documentation migration scaffolding and controls.

Current artifacts:
- `classification-rubric.md`: deterministic category rules and path projection policy.
- `inventory.json`: migration inventory with category assignments and proposed targets.

Target taxonomy entry points used by the migration:
- `docs/design/`
- `docs/history/`
- `docs/planning/`
- `docs/decisions/`
- `pseudo/` for non-integrated pseudo-code and prototypes.
